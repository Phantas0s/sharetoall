# This file is auto-generated during the composer install
parameters:
    app.version: '%env(APP_VERSION)%'
    app.domain: '%env(APP_DOMAIN)%'
    app.url: '%env(APP_URL)%'
    app.redirect_uri: '%env(APP_REDIRECT_URI)%'
    app.email: '%env(APP_EMAIL)%'
    app.locale: '%env(APP_LOCALE)%'
    db.host: '%env(DB_HOST)%'
    db.port: '%env(DB_PORT)%'
    db.name: '%env(DB_NAME)%'
    db.user: '%env(DB_USER)%'
    db.password: '%env(DB_PASSWORD)%'
    redis.host: '%env(REDIS_HOST)%'
    redis.port: '%env(REDIS_PORT)%'
    mailer.host: '%env(MAILER_HOST)%'
    mailer.port: '%env(MAILER_PORT)%'
    mailer.encryption: '%env(MAILER_ENCRYPTION)%'
    mailer.username: '%env(MAILER_USERNAME)%'
    mailer.password: '%env(MAILER_PASSWORD)%'
    mailer.auth_mode: '%env(MAILER_AUTH_MODE)%'
    feature.example: '%env(FEATURE_EXAMPLE)%'
    minified_js: '%env(MINIFIED_JS)%'
    minified_css: '%env(MINIFIED_CSS)%'
    twitter.consumer_key: '%env(TWITTER_CONSUMER_KEY)%'
    twitter.consumer_secret: '%env(TWITTER_CONSUMER_SECRET)%'
    linkedin.consumer_key: '%env(LINKEDIN_CONSUMER_KEY)%'
    linkedin.consumer_secret: '%env(LINKEDIN_CONSUMER_SECRET)%'
