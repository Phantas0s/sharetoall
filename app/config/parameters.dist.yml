# This file is auto-generated during the composer install
parameters:
    app.version: '%env(APP_VERSION)%'
    app.domain: '%env(APP_DOMAIN)%'
    app_url: '%env(APP_URL)%'
    app_redirect_uri: '%env(APP_REDIRECT_URI)%'
    app_email: '%env(APP_EMAIL)%'
    app_locale: '%env(APP_LOCALE)%'
    db_host: '%env(DB_HOST)%'
    db_port: '%env(DB_PORT)%'
    db_name: '%env(DB_NAME)%'
    db_user: '%env(DB_USER)%'
    db_password: '%env(DB_PASSWORD)%'
    redis_host: '%env(REDIS_HOST)%'
    redis_port: '%env(REDIS_PORT)%'
    mailer_host: '%env(MAILER_HOST)%'
    mailer_port: '%env(MAILER_PORT)%'
    mailer_encryption: '%env(MAILER_ENCRYPTION)%'
    mailer_username: '%env(MAILER_USERNAME)%'
    mailer_password: '%env(MAILER_PASSWORD)%'
    mailer_auth_mode: '%env(MAILER_AUTH_MODE)%'
    feature_example: '%env(FEATURE_EXAMPLE)%'
    minified_js: '%env(MINIFIED_JS)%'
    minified_css: '%env(MINIFIED_CSS)%'
    twitter_consumer_key: '%env(TWITTER_CONSUMER_KEY)%'
    twitter_consumer_secret: '%env(TWITTER_CONSUMER_SECRET)%'
    linkedin_consumer_key: '%env(LINKEDIN_CONSUMER_KEY)%'
    linkedin_consumer_secret: '%env(LINKEDIN_CONSUMER_SECRET)%'
